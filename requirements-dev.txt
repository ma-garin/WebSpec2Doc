# 開発・CI 用ツール（ランタイム依存は requirements.txt）
-r requirements.txt

ruff==0.6.9
black==26.3.1
mypy==1.13.0
types-PyYAML==6.0.12.20260518
pip-audit==2.7.3

# Pillow / numpy は web/services/visual_complexity.py が実行時に使うため
# requirements.txt（ランタイム依存）へ移動した（E2E ピクセル比較でも使用）
