# 開発・CI 用ツール（ランタイム依存は requirements.txt）
-r requirements.txt

ruff==0.6.9
black==24.10.0
mypy==1.13.0
pip-audit==2.7.3

# E2E ビジュアルリグレッションのピクセル比較（未導入だと完全hash一致を要求し
# PNGエンコードの非決定性で誤検知するため、verify-ui の堅牢化に必須）
Pillow>=11.0.0
numpy>=2.0.0
