# UX ベースライン実測（P0-1）

- 対象: `http://127.0.0.1:8767/index.html`
- 試行回数: 3 回（中央値を基準値とする）
- 体感ではなく実測値のみ。改善施策の前後比較に用いる。

| 指標 | 中央値 | 最小 | 最大 | 補足 |
|---|---|---|---|---|
| 画面解析（discover） | **2.36 秒** | 2.34 秒 | 3.02 秒 | 7 画面 |
| 全画面クロール（成果物生成まで） | **16.61 秒** | 16.50 秒 | 16.62 秒 | 出力先 127.0.0.1:8767 |
| 画面別設計 API | **0.00 秒** | 0.00 秒 | 0.03 秒 | 4 画面 |

## 生データ（秒）

- 画面解析（discover）: 3.02, 2.36, 2.34
- 全画面クロール（成果物生成まで）: 16.50, 16.61, 16.62
- 画面別設計 API: 0.03, 0.00, 0.00

## 追加計測: レポート初回表示（P1-3）

`#report/<domain>` へ遷移してから概要タブの主要パネル（画面インベントリ表）が
描画されるまでを Playwright で計測（既存の解析済みサイト・3 回）。

| 指標 | 中央値 | 生データ（秒） |
|---|---|---|
| レポート初回表示 | **0.95 秒** | 1.02, 0.95, 0.95 |

プラン P1-3 の発動条件は「1.5 秒超過」。**下回ったため改善には着手せず、記録のみで完了**とする。
