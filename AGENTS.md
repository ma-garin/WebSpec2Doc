# AGENTS.md — WebSpec2Doc 作業ガイド

常に日本語で回答してください。

## 進行中の作業（着手前に必ず確認）

- **テスト技法エンジンの研究ベース更新**: Wave R（先行研究調査）と Wave 1（組合せ統合・
  偽ペアワイズ修正）まで完了、Wave 2 以降が未着手。
  引き継ぎ書 → [docs/HANDOFF_technique-engine.md](docs/HANDOFF_technique-engine.md)
  （文脈ゼロから再開できるように、既知バグ・設計原則・ウェーブ計画・前後比較手順を集約してある）

## クレジット節約方針（明示依頼がない限り順守）

- まず最小限の調査で原因・方針だけを簡潔に報告する。
- 大きなファイル全文取得・広範囲検索・大きな API レスポンス取得を避ける。
- 2分以上かかりそうな調査・作業は、中断してユーザーに確認する。
- 修正・コミット・push・デプロイまで進める場合は、ユーザーの明示的な依頼または承認を得る。

## GitHub 証跡の恒久ルール

- 本リポジトリではユーザー承認済みの恒久ルールとして、実装・修正の完了時は個別の再承認を待たず、コミット、topic branch へのpush、PR作成、CI確認、mainへのsquash merge、ローカルmain同期まで行う。
- 長時間作業の中断・セッション終了が近い場合は、追跡可能なcheckpoint commitを作成し、topic branchへpushする。未完成の変更はmainへマージしない。
- 無関係な差分はPRに含めず、CIが失敗した状態ではマージしない。

## 開発速度の規律（2026-08-01 実測に基づく・順守必須）

**目標: 同じ成果を今までの半分の時間で出す。** 以下は 2026-08-01 の実測
（PR #128〜#135・約6時間）から、無駄と特定できた工程への対処である。
削るのは工程そのものではなく、**重複実行と手戻り**。

### S-1. 実装の前に、実データを 1 回だけ叩く（最優先）

新しい API・データ構造に触れる前に、**実物を 1 回取得して形を確認する**。

```bash
curl -s "http://127.0.0.1:8765/api/<endpoint>?..." | venv/bin/python -m json.tool | head -30
```

理由: 実測した遅延の最大要因は「実装 → 実データで確認 → **前提が違うと判明** → 作り直し」。
2026-08-01 だけで 3 件・約 50 分を失った。

| 実際に起きた誤り | 1 回の確認で防げたか |
|---|---|
| `/api/snapshots` がオブジェクト配列（文字列配列だと誤認） | 防げた |
| `/preview` が cwd 基準でパス解決（相対パスで 404） | 防げた |
| severity が `breaking/warning/info`（`high/medium/low` だと誤認） | 防げた |

**着手時のコストは 2〜3 分。手戻りは 15〜25 分。**

### S-2. `make verify-ui` はコミット直前の 1 回だけ

2026-08-01 は 8 回実行し、1 回 2〜3 分で**約 20 分**を消費した。
`.ui-verified` は 1 時間有効なので、途中で回しても無駄になる。

- 実装中は `make test`（20秒）だけで回す
- `verify-ui` は**コミットする直前**に 1 回
- UI ファイルを変更したら、その後にもう一度コミットする場合は再実行が要る
  （ハッシュ照合で弾かれる）ため、**UI の編集を全部終えてから**回す

### S-3. コミット前の `make test` を省略する

pre-commit hook が同じ pytest を実行する。手前で流すと**同じテストを二重に走らせる**。
`make test` は実装中の確認に使い、コミット直前には流さない。

### S-4. ブラウザ検証は「測定方法の失敗」を疑う

2026-08-01 は、タブが切り替わらない・画面が描画されない事象が 3 回起きたが、
**いずれも実装ではなく検証コードの問題**だった（初期化完了前の操作・古い JS のキャッシュ）。
合計約 15 分を失った。

- 画面が出ないときは、まず `location.hash` と対象要素の `hidden` を見る
- サーバ再起動後は**必ずページを再読み込み**してから操作する
- 2 回失敗したら実装を疑う前に、状態を 1 回ダンプする

### S-5. 報告は「実測表・判断・弱点」だけにする

経緯・試行錯誤・採らなかった案の詳細は書かない。出力は逐次生成されるため、
長さがそのまま待ち時間になる。

### S-6. 開発時はテンプレート自動リロードを有効化する

Flask は `debug=False` でテンプレートをキャッシュするため、UI 変更のたびに
サーバ再起動が要る。2026-08-01 は 7 回再起動し**約 10 分**を消費した。

```bash
WEBSPEC2DOC_TEMPLATES_AUTO_RELOAD=1 venv/bin/python app.py --no-browser
```

これを付けると次の 2 つが同時に効く（2026-08-01 実測で確認済み）:

- テンプレートの変更が**再起動なしで反映される**
- `?v={{ _ver }}` が毎リクエスト変わり、**JS / CSS の古いキャッシュを掴まない**

既定（環境変数なし）は従来どおり、起動時に固定した `_ver` を配ってブラウザに
キャッシュさせる。**本番では付けない。**

観点カタログ（`data/viewpoint_*.json`）もこのとき再読込される。更新時刻を見て
実際に編集されたときだけキャッシュを捨てるので、付けても無駄な再計算はしない。

---

## 検証の規律

2026-08-01 のレッドチーミングで 8 件の欠陥が出た。原因は知識不足ではなく、
**検証を打ち切る位置が一貫して手前だった**こと。同じ形の誤りが 4 回、2 回、
2 回と繰り返されたので、気をつけるのではなく手順として固定する。

### V-1. 「渡した」で検証を終えない。出力の実物を見る

観点を QA 層へ渡すようにして「届いた」と報告したが、生成される Markdown は
`.name` しか使っておらず、期待結果は固定文言で上書きされていた。同じ形の
誤りが 1 日で 4 回出ている（E2E が緑＝繋がっている／定義を直した＝反映された／
隔離を実装した＝隔離されている／QA 層に渡した＝文書に出る）。

**値が最終的な成果物に現れることをテストで固定する。** 中間層の戻り値を
検査するテストは、下流が値を捨てていても緑のままになる。

```python
# 不十分: QA 層の戻り値を見ている
assert any(vp["expected_result"] for vp in _load_qa_viewpoints())

# 十分: 生成された文書の本文を見ている
assert item["expected_result"] in doc_generator._test_design(domain, report)
```

### V-2. 「消えない」を直したら「消える」も試す

「予約語と衝突しても品質領域が消えない」ようにしたが、逆方向（通常の観点が
見出し扱いされて設計表から消える）が残っていた。片側だけ直して「直った」と
報告した例が 2 件ある。

**対称性のある不具合は、両方向のテストを同じコミットに入れる。** さらに、
同じ判定が複数箇所に散っていたら、そこが再発の温床である。判定は 1 箇所で
行い、以降はその結果だけを見る（`_role_and_area` はこの形にした）。

### V-3. 検証を走らせる前にコミットし、終わるまで触らない

レッドチームを未コミットの作業ツリーで 2 回走らせ、2 回とも「対象が検証中に
書き換わった」と報告された。1 回目の指摘を受けた直後に再発している。
検証対象が動くと、報告された結果がどの状態のものか分からなくなる。

**手順**: `git status --short` が空であることを確認 → 検証を起動 →
完了通知まで**リポジトリ内のファイルを一切編集しない**。
待つ間に進めたいことがあれば、scratchpad で行う。

### V-4. 「崩せなかった」は1回の実行では言わない

並行処理の検証を1回走らせて「崩せなかった」と報告したが、実際は
**6回に1回失敗する**不具合が潜んでいた（`PRAGMA journal_mode = WAL` が
他の接続と競合して `database is locked` を返す）。全体実行で顕在化して
初めて気づいた。

同じ形の誤りが 1 日で 3 回出ている（E2E が緑＝繋がっている／隔離を
実装した＝隔離されている／並行アクセスは崩せなかった）。

**タイミングに依存する検証は繰り返す。** 並行処理・ポート確保・
ファイル監視のように結果が揺れうるものは、20 回程度連続で実行して
失敗ゼロを確かめてから「崩せなかった」と書く。

```bash
fail=0; for i in $(seq 1 20); do
  venv/bin/python -m pytest <対象> -q 2>&1 | tail -1 | grep -q passed || fail=$((fail+1))
done; echo "失敗: $fail / 20"
```

### V-5. 異常値を入れて確かめる

「今のデータは壊れていない」ことの確認は、「壊れたデータを与えたら
どうなるか」の答えにならない。カタログは人が編集する JSON で、
型の取り違えは起こる。実際、能力タグに配列でなく文字列を入れると
`"core"` が `{"c","o","r","e"}` に分解され、適用定義が **89 → 0** に
なるのに、エラーも警告もなく「観点 0 件のセット」ができていた。

**壊れた入力に対しては、どれが悪いかを名指しして落とす。** 黙って
既定値へ倒すと、それらしい出力が出てしまい、利用者は気づけない。

### やらないこと（速度のために品質を落とさない）

- `verify-ui` 自体の省略（ゲートは維持する。回数だけ減らす）
- フェーズごとのコミットをまとめる（切り戻し単位が粗くなり、設計変更時に損をする）
- 実データ確認の省略（S-1 は削減策であって、削る対象ではない）

## UI変更時の必須フロー（機械的ゲート付き）— INC-2026-001 是正済み

UI（HTML / JS / CSS）を変更する場合は、必ず次の順で進める。
**省略は INC-2026-001 相当のインシデントとして扱う。**

1. **スキャン**: 変更対象・影響範囲・既存UI構成を把握する。
2. **提示**: 実装方針・期待される画面状態をユーザーへ提示する。
3. **合意**: ユーザーの合意を得てから実装する。
4. **L1/L2 テスト**: `make test` → 全 PASS を確認する。
   実装中はこれで回す。**コミット直前には流さない**（pre-commit が同じものを実行するため。S-3）。
5. **L3 E2E テスト（機械的強制・省略禁止）**: `make verify-ui` を実行する。
   - E2E テストが全 PASS → `.ui-verified` マーカーが自動生成される
   - FAIL → コードを修正してから再実行する（コミット不可）
   - **UI ファイルの編集を全部終えてから、コミット直前に 1 回だけ実行する**（S-2）。
     途中で回してもハッシュ照合で無効になり、2〜3 分がそのまま無駄になる。
6. **スクリーンショット確認**: `tests/e2e/screenshots/` の画像を目視確認する。
7. **解像度確認**: 1920×1080 / 1366×768 の両方でブラウザを確認する。
8. **フロー確認**: 変更した機能のユーザーフローを最初から最後まで通して操作する。
9. **コミット**: `git commit` → pre-commit hook が自動的にゲートを確認する。
10. **最終報告**: E2E 結果・スクリーンショット・目視確認内容を明記して報告する。

### ゲートの強制構造（人間の判断に依存しない）

```
make verify-ui
  → Flask 起動 → E2E テスト実行 → 全 PASS → .ui-verified 生成
                                   ↓ FAIL
                                コミット不可（修正必須）
git commit
  → pre-commit hook
       → .ui-verified が存在 かつ 1時間以内 → PASS → コミット完了
       → 不在 or 古い                        → BLOCKED
```

**参照**:
- テスト戦略: [docs/TESTING_STRATEGY.md](docs/TESTING_STRATEGY.md)
- 完了の定義: [docs/DEFINITION_OF_DONE.md](docs/DEFINITION_OF_DONE.md)
- インシデント記録: [docs/INCIDENT_POSTMORTEM.md](docs/INCIDENT_POSTMORTEM.md)

## 進捗バー（応答末尾の慣習）

毎回の応答末尾に進捗を視覚表示する。NN% はロードマップ達成数から正直に算出する。

```
WebSpec2Doc  ████████░░ NN%
[x] 完了マイルストーン
[ ] 未着手
```

## Codex への委任

- 仕様が固まった実装は Codex に委任してよい。セッションは必ず正本ディレクトリ `/Users/fujimagariyuki/dev/active/webspec2doc` で開く（旧コピーは 2026-07-03 に統合済み・参照禁止）。
- 委任後の成果物は**必ずレビュー**する（過去に未配線スタブ混入の事例あり）。

## 検証とドキュメント

- 変更後は `./scripts/verify.sh`（py_compile + pytest + スモークパイプライン）を実行。
- アーキテクチャ・拡張手順・ロードマップ・ハマりどころは **[docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)** を参照。
