# 0705 再ドッグフィードバック対応計画（Round 2）

作成日: 2026-07-05 ／ ステータス: 承認待ち → 実装中に更新
前提: docs/0703-01_plan.md（WBS）の後続。第2ラウンドのドッグフーディング（hotel-example-site、スクショ4枚・約30項目）への対応計画。

## Context

核心は次の2点。

1. **AutoRun全テスト失敗**（P0環境バグ）
2. **前回フィードバックの未反映項目への憤り** — 検証の結果、Sprint 2送りにした項目・指示と違う形で実装した項目・対象を取り違えた項目が実在する

信頼回復（前回未反映の完遂）を最優先に、3スプリントで全項目に対応する。前ターン受領の「ドキュメント刷新＋大会関連文書の削除＋日英非エンジニア向けガイド」もSprint 3に含める。

ユーザー確認済みの要件解釈:

| 項目 | 確認結果 |
|---|---|
| 機能一覧表 | **解析対象サイトの機能一覧**（画面・フォーム・遷移から導出する新成果物） |
| ヒートマップ | **カバレッジヒートマップ**（解析=画面の取得状況、AutoRun=画面ごとの実行回数・成否） |
| ショートカット | 標準セットにお任せ |
| 「ライブプレビューを並列処理に」 | 真意は**AutoRunテスト実行中のライブプレビュー表示**（クロール並列数の既定値は変更しない） |

## 検証済み根本原因（P0）

### P0-1: AutoRun 全テスト失敗 — ✅対応済み

実測: `output/.playwright_env/package.json` = `@playwright/test ^1.56.1`、Python側 `playwright==1.44.0`。`_ensure_pw_env`（web/services/playwright_executor.py:338-369）が `npx playwright --version`（環境の最新）でバージョン決定→不一致。1.56は `chromium_headless_shell-1217` を要求、`.runtime/ms-playwright` には1.44用chromiumのみ。

**修正（実装済み）**:
- (a) `importlib.metadata.version("playwright")` にピン止め（package.jsonにdependencies記録。当初案の`playwright.__version__`は実在しない属性のため方式変更）
- (b) 既存envのバージョン不一致検出→`node_modules`削除＋再構築
- (c) 実行前ブラウザ実在検証・無ければ `npx playwright install chromium`（PLAYWRIGHT_BROWSERS_PATH明示）で自動修復
- (d) failure_classifier に「実行環境エラー」パターン追加（browserType.launch/Executable doesn't exist）＋同一原因の大量重複失敗を1件に集約（`count`/`affected_test_ids`）→ 334件未分類ではなく1件の環境エラー＋修復手順として表示

**検証**: 新規ユニットテスト31件追加（`tests/test_playwright_executor.py`・`tests/test_failure_classifier.py`）で全て green。さらに実際に `run_playwright()` をライブ実行し、(1) 環境未構築の初回実行、(2) バージョン不一致（1.56.1→1.44.0）からの再構築、の両シナリオでnpmインストール・ブラウザ実在確認・実ブラウザでのテスト実行が完走することを確認済み。

### P0-2: 認証情報入力パネルの潰れ（画像1）

実測: パネル本体レイアウトが100%インラインstyle依存（`.disc-item-login-body` のCSSは**空ルール** static/app-report.css:271）、外枠 `.disc-login-item-wrap` が `overflow:hidden` → 高さ喪失時にオレンジ縞に潰れる構造。

**修正**: パネルにbase CSS（display/flex/min-height）を付与しインライン依存を解消、`overflow:hidden` を見直し、描画高をアサートするE2E追加。

## 前回指摘の未反映項目（検証済み・今回完遂）

| 前回指摘 | 実態 | 対応 |
|---|---|---|
| 品質観点: 画面ID/画面/リスク/理由にiマーク | 別テーブルに付け対象違い | 画面リスク表にiマーク＋画面の目的/見方の説明ブロック＋用語説明 |
| 観点管理: ISTQB/ISO25010/非機能要求グレード2018/PMBOKプリセット | テンプレは空フォルダのみ（viewpoints.js:30-34） | 実観点アイテム入りプリセットを `data/viewpoint_templates/*.json` として実装 |
| 観点管理: テスト値カタログ（メール: 上限値/上限値+1/空白/2byte/RFC違反/未登録/解約済み等） | 未実装 | カタログ実装（BのMBT設定と同一ストアで一度だけ定義） |
| テストケース一覧は別画面で | モーダル内展開行で代替 | 専用ビュー新設（A） |
| テストケース詳細 | playwrightコード行を表示 | 日本語ローレベルケース（前提/手順/期待結果）へ決定的変換（A） |
| 実行ライブプレビュー | Sprint 2送り | AutoRun実行中ライブプレビュー実装 |
| 条件マトリクス/テスト設計が弱い | 未対応 | テスト設計タブ刷新（B）に統合 |

## Sprint 1 — 信頼回復（P0＋前回未反映）

1. **P0-1 Playwright環境修正**（最初。全AutoRun作業のブロッカー）＋実際にAutoRunを完走させて確認
2. **P0-2 認証パネルCSS根治**＋E2E
3. 品質観点: 画面リスク表iマーク＋説明ブロック（static/js/qa-tools.js:216周辺）
4. **観点プリセット＋テスト値カタログ**: `data/viewpoint_templates/{istqb,iso25010,nfr2018,pmbok}.json`（実アイテム入り）＋読込API。テスト値カタログは `instance/test_design_settings.json` の value_catalog として実装（Bが後で消費）
5. **AutoRunライブプレビュー**: `/api/live-screenshot` 方式を `qa_process/test-results/**.png`（config済み screenshot:'on'）に向けた新エンドポイント＋running_tests時のUI表示
6. 設定改修: モデルタブ廃止→APIキータブに統合（キー＋モデル＋**疎通確認** `POST /api/settings/test-connection`、src/llmプロバイダでping）。クロール既定値タブは存続
7. AutoRun深さ/最大ページ: 既定=上限（全対象）、「詳細オプション」折りたたみへ
8. 中断ボタンのスタイル修正（小型化・控えめに）

## Sprint 2 — 新規UX

1. **探索カバレッジ削除を最初に**（templates/partials/view-generate.html:299,338 / static/js/results.js:17 / static/js/view-coverage.js / tests/e2e/test_coverage_tab_e2e.py を同一コミットで更新。バックエンド生成系は残置）— Bとのタブバー競合回避
2. **A: テストケース専用画面＋ローレベル化＋HTMLレポート**
   - 新モジュール `web/services/qa/testcase_docs.py`: `LowLevelCase`(frozen) / `translate_step()`（page.goto→「ブラウザでUを開く（画面名）」等の決定的regex変換表。日本語プローズ行はそのまま。捏造なし）/ `generate_testcases_html()`（印刷対応・自己完結HTML）
   - `GET /api/testcases?domain=`（web/routes/qa_process.py）、成果物 `qa_process/testcases.html` を advanced_generator から生成
   - 新ビュー `templates/partials/view-testcases.html`＋`static/js/view-testcases.js`（ナビ「テストケース」、承認モーダルの「テストケース一覧を確認する」→本ビューへ遷移＋戻るバナー）
3. QAレポートmd生表示修正（web/services/qa/doc_generator.py:392-396 の `<pre>`埋め込み→サーバーサイドmd→HTML変換）＋成果物見切れCSS（static/css/components.css:572-581 折返し許可＋title）＋SDLC分類（成果物リストを計画/分析/設計/実装/実行/レポートでグルーピング）
4. **C: 実行履歴の一般化**: web/services/usage_tracker.py に `record_autorun()`（terminal時にstatus/passed/failed/total/duration記録）、`GET /api/history/runs`（usage_log.jsonl＋実行中ジョブのマージ、リンクは実在検証）、新ビュー `view-run-history.html`（種別バッジ/主要数値/ディープリンク/フィルタ）。`#nav-run-history-btn` の遷移先を差し替え
5. 小物:
   - ギャラリー一括エクスポート（チェックボックス＋全選択/解除＋`/download-zip` paths対応）
   - 画面と仕様のライトボックス_full.png化（static/js/view-report.js:44-47）
   - シナリオ表QA観点具体化（static/js/view-transition.js:176→toTitle/eventDetail使用）
   - **機能一覧表**（画面・フォーム・遷移から機能を導出する新成果物 features.md＋レポート表示。画面一覧は既存 screens.md／概要タブをレポート内で見つけやすく明示配置）
   - キーボードショートカット標準セット拡張（1〜9タブ・Alt+A/H・/検索等＋ヘルプ全掲載）
   - テスト観点マップに凡例・見方の説明ヘッダを追加（R1指摘「見方がわかりにくい」対応）
   - AutoRunの観点セット選択を「目的別」表示に（Sprint 1-4のプリセット観点セットを目的グルーピングで提示。R1指摘のUX_Auto_Reviewer風表示対応）

## Sprint 3 — 大型機能＋ドキュメント

1. **B: テスト設計タブ（MBT）**
   - エンジン `src/generator/test_design.py`（純関数）: `TestDesignParams`(frozen: enabled_techniques/bva_offset/pairwise_strength 2|3/n_switch 0|1/max_dt_conditions/value_catalog) → `build_test_design(report, params)`。evidence-only（実測1.0/カタログ値0.9バッジ）。ペアワイズは自前貪欲IPO（決定的）、Nスイッチは遷移グラフ経路列挙
   - 設定: `instance/test_design_settings.json`＋`GET/POST /api/settings/test-design`＋設定「テスト設計」タブ（技法ON/OFF・パラメータ・値カタログ編集表）
   - UI: test-designタブに「技法別設計」サブタブ追加（**matrixは既定のまま**→既存E2E無傷）。技法チップ→対象画面リスト→クリックでモーダル（BVA表/DT真理値表/PW行/ST系列＋根拠＋パラメータ表示）
2. **D: 履歴・差分の現新比較化**: src/diff/comparison.py から `compare_analyzed_pages(old_pages, new_pages, *, dynamic_masks, check_links=False...)` を抽出（ライブ経路はバイト同一の挙動）。`GET /api/snapshot-comparison` → `generate_comparison_html` を iframe.srcdoc 表示。モード切替ラジオ「現新比較（4分類・既定）／簡易ドリフト差分」で旧diffはフォールバック存続
3. フローチャート追加（static/js/view-transition.js:208-210/451-453 に flowchart サブタブ、既存Mermaidパターン踏襲）
4. **カバレッジヒートマップ**: 解析=画面マップ上に取得/未取得/要ログインを色分け、AutoRun=画面ごとの実行回数・成否を色分け（src/generator/heatmap_reporter.py のバケットパターン再利用、レポート内表示）
5. **ドキュメント刷新**:
   - 削除: docs/01〜06_*.html＋index.html（ピッチ資料一式）、07_商用化戦略.md/.html、08_プロダクト機能戦略.md、09_事業計画_統合版.md、10_社内ベンチャー準備タスク.md、demo/PITCH_OUTLINE.md、archive/SELLABLE_PRODUCT_PLAN.md、archive/plan.txt
   - 編集: docs/11ロードマップ（§4事業面の含意＋大会文言を除去し開発ロードマップとして存続）、demo/DEMO_SCRIPT.md（大会ヘッダ・ピッチ締め文言を汎用化）、docs/README.md（事業セクション削除・索引再構成）、README.md（リンク修繕＋非エンジニア向け冒頭）、DEVELOPMENT.md（09参照を製品目的説明に置換）
   - 新規: **docs/GUIDE_ja.md / docs/GUIDE_en.md**（非エンジニア向け: 何ができるか・使い方を専門用語なしで）＋README_en.md

## 競合・整合の要点

- テスト値カタログ（Sprint 1）とBのvalue_catalogは**同一ストア**で一度だけ定義
- 条件マトリクス改善はBのPRに統合（同一タブ・同一E2E）
- 承認モーダルの既存テーブルは残し、Aの専用画面への導線ボタンを追加（レンダラ二重化しない）
- 探索カバレッジ削除は test_report_tabs_e2e.py のタブ数アサーションを同一コミットで更新
- Dで旧簡易diffを残すことで r-diff-badge / CI --fail-on-drift 導線は非破壊

## 検証

- P0-1は**実際にAutoRunを実行してテストが完走すること**を確認（環境自動修復含む）
- 各コミットで全ゲート: black(**Python3.12実機**)/ruff/mypy/bandit/pytest（L1/L2）/quality_harness/make verify-ui
- 新規E2E: 認証パネル描画高、テストケース画面、実行履歴、MBT設計モーダル、スナップショット現新比較、設定疎通確認（ポート8912〜割当）
- スプリント単位でコミット→push→PR→CI green→即マージ（確立済みパターン）

---

## 付録: 全指摘トレーサビリティ表（Round 1＋Round 2 全64項目・実装詳細つき）

凡例: ✅=対応済み（完了）／ 未対応行には**対応方法**列に file:line 込みの具体的実装手順を記載（Sonnetでもそのまま着手可能なレベル）。⚠️=本計画スコープ外（理由明記）。

### Round 1（2026-07-05 午前・約35項目）

| # | 指摘 | 状態 | 対応方法（未対応のみ） |
|---|---|---|---|
| R1-01 | 【解析】ログインフォームが出てこない（再発領域） | ✅→再発 **未対応（S1-2）** | `static/app-report.css:271` の空ルール `.disc-item-login-body{}` に `display:flex;flex-direction:column;gap:8px;min-height:64px` を追加。`.disc-login-item-wrap`（同ファイル:266）の `overflow:hidden` を `overflow:visible` に変更（縞状クリップの直接原因）。E2E: `static/js/wizard.js` の discover 結果に要ログイン画面を含むフィクスチャを与え、`.disc-item-login-panel` の `getBoundingClientRect().height` が例えば100px以上であることをアサートするテストを `tests/e2e/test_discover_login_panel_e2e.py` に追加 |
| R1-02 | 【解析】差分表示ボタンの反応がわからない | ✅ | ローディング/エラー表示追加済み |
| R1-03 | 【解析】遷移図/遷移表のタブinタブ構造 | ✅ | セグメントコントロール化済み |
| R1-04 | 【解析】テスト設計が弱い・条件マトリクスがわかりにくい | **未対応（S3-1 B）** | 本文「Sprint 3 - 1. B: テスト設計タブ（MBT）」節を参照。`src/generator/test_design.py` の `build_test_design()` が `static/js/view-design.js:21-114` の `_recommendFor` ロジックを移植・拡張する |
| R1-05 | 【解析】テスト観点マップの見方がわかりにくい | **未対応（S2-5）** | `static/js/view-transition.js` のシーケンス図表示部（`_sequenceDiagram`、:456-468付近）の直前に凡例ブロックを追加: `<div class="tx-legend"><span>→ 画面遷移</span><span>◇ 分岐</span><span>QA観点=遷移条件の確認ポイント</span></div>`。CSS新規クラス `.tx-legend` を `static/css/report-v2.css` に追加（小型・グレー背景・アイコン付き） |
| R1-06 | 【解析】ギャラリー画像が見切れ（全画面取得でない） | ✅（ギャラリー側）／画面と仕様側は**未対応（S2-5）** | `static/js/view-report.js:44-47` に `static/js/view-shots.js:16-20` の `fullByPageId` マップ構築ロジックを移植し、ライトボックス（`onclick="openLightbox(...)"`）には `_full.png` を優先して渡す。サムネイル表示（`<img>`のsrc）はビューポート版のまま据え置く |
| R1-07 | 【解析】遷移先がP003で分かりにくい | ✅ | 画面名表示に変更済み |
| R1-08 | 【AutoRun】深さ・最大ページが直感的でない | ✅（iマーク）→ **未対応（S1-7: オプション化）** | 本文 Sprint 1-7 参照。`templates/partials/view-auto-run.html:26-32` の depth/max-pages 入力を `<details>` で「詳細オプション」に格納し、既定値を `MAX_DEPTH`/`MAX_PAGES_LIMIT`（`web/config.py:19-20`）まで引き上げる |
| R1-09 | 【AutoRun】観点セットを目的別/ペルソナ風表示に | **未対応（S2-5）** | `templates/partials/view-auto-run.html` の `#autorun-viewpoint-set` セレクトを `<optgroup label="機能テスト重視">` 等でグルーピング。グループ分けはS1-4で作る `data/viewpoint_templates/*.json` の `category` フィールド（istqb→テストレベル別、iso25010→品質特性別等）をそのままoptgroupラベルに利用する |
| R1-10 | 【AutoRun】ログイン情報をまとめて提示 | ✅ | ドメイン単位1回に統合済み |
| R1-11 | 【AutoRun】テストケース一覧は別画面で | 指示と相違（モーダル内展開で代替）→ **未対応（S2-2 A）** | 本文「Sprint 2 - 2. A: テストケース専用画面」節を参照。新規 `templates/partials/view-testcases.html` ＋ `static/js/view-testcases.js`、ナビ項目「テストケース」を追加し、承認モーダルの「テストケース一覧を確認する」ボタンから遷移させる |
| R1-12 | 【AutoRun】テストケース一覧の詳細が見られない | 部分対応（コード行表示のみ）→ **未対応（S2-2 A）** | `web/services/qa/testcase_docs.py` を新設し `translate_step()` で `page.goto/click/fill` 等のコード行を日本語の手順文へ決定的変換（正規表現テーブル、捏造なし）。`LowLevelCase`（frozen dataclass）に前提条件/手順/期待結果を持たせ、専用画面で表示 |
| R1-13 | 【AutoRun】制限時間のiマーク | ✅ | 追加済み |
| R1-14 | 【AutoRun】実行ログを非エンジニア向けに | ✅ | 生ログを開発者トグルへ分離済み |
| R1-15 | 【品質観点】画面の目的と見方がわからない | **未対応（S1-3）** | `static/js/qa-tools.js:216` 付近（画面リスク表描画箇所）の直前に説明ブロック `<div class="qa-explain">この表は各画面の操作要素数とリスクスコアから、テストの優先順位を判断するためのものです。</div>` を挿入 |
| R1-16 | 【品質観点】操作要素あり/リスクの意味がわからない | **未対応（S1-3）** | 同箇所のテーブルヘッダ（`<th>操作要素</th><th>リスク</th>`）に `infoTip()`（`static/js/core.js` 既存ヘルパー、`static/js/qa-tools.js` の既存iマーク実装パターンを流用）を追加。説明文例:「操作要素=クリック可能なボタン・リンクの数（多いほど確認すべき操作パターンが増える）」「リスク=入力項目数・必須項目数・遷移先数から機械的に算出したスコア（実測データのみ・LLM未使用）」 |
| R1-17 | 【品質観点】画面ID/画面/リスク/理由にiマーク | 対象違い（発火条件等の別テーブルに付与）で未達 → **未対応（S1-3）** | `qa-tools.js` の画面リスク表（`renderQaModelTool`内、advanced_html.py側ではなくJS側で画面ID/画面/リスク/理由の4列を持つテーブル）の各 `<th>` に R1-16 と同様の `infoTip()` を追加 |
| R1-18 | 【観点管理】ISTQB/ISO25010/非機能要求グレード2018/PMBOKプリセット | 空フォルダのみ（`static/js/viewpoints.js:30-34` の `VP_TEMPLATES` は folders 名のみで観点アイテムなし）→ **未対応（S1-4）** | `data/viewpoint_templates/istqb.json` 等4ファイルを新設し、各テンプレートに実際の観点アイテム（id/viewpoint/trigger/recommendation/automation/category）を最低20件程度収録。`web/services/viewpoint_store_operations.py` に読込関数を追加、`GET /api/viewpoints/templates/<key>` で返す。フロント `viewpoints.js:919` の `data-template` クリックハンドラを、フォルダのみ作成する現行ロジックから「フォルダ作成＋アイテム一括投入」に変更 |
| R1-19 | 【観点管理】テスト値カタログ（上限値/上限値+1/空白/2byte/RFC違反/未登録/解約済み等） | **未対応（S1-4）** | `instance/test_design_settings.json` に `value_catalog: {email: {valid:[...], invalid:["空欄","101文字（上限+1）","user@invalid","２バイト文字（全角）"]}, ...}` を持たせる。`web/services/test_design_settings.py` を新設し load/save。設定画面「テスト設計」タブ（S3-1で追加）に編集UIを置くが、**データ自体はS1-4でここで先に作る**（S3-1のvalue_catalogパラメータと同一ストアを共有） |
| R1-20 | 【ガイド】スクロールできない | ✅ | is-executing残留の根治済み |
| R1-21 | 【ガイド】CLIの言及がない | ✅ | CLI節追加済み |
| R1-22 | 【設定】タブ操作ができない | ✅ | ハンドラ追加済み |
| R1-23 | 【全体】実行履歴をサイドメニューから | ✅（ナビ追加のみ）→中身の一般化は**未対応（S2-4 C）** | 本文「Sprint 2 - 4. C: 実行履歴の一般化」節を参照 |
| R1-24 | 【全体】ホームでTest Design/AutoRunのモード選択 | ✅ | モードカード化済み |
| R1-25 | 【全体】ガイド付き解析→Test Design改名 | ✅ | 済み |
| R1-26 | 【全体】テスト設計をユーザーがアレンジしたい | **未対応（S3-1 B の設定タブ）** | S3-1の `GET/POST /api/settings/test-design` と設定「テスト設計」タブで、技法ON/OFF・境界値オフセット・ペアワイズ強度・値カタログを調整可能にする。個別ケースの手編集（例: 生成された1件のテストケースの期待結果を書き換える）はB完了後の次期候補として明示的にスコープ外（データモデルに手動上書きフィールドを追加する設計が別途必要） |
| R1-27 | 【全体】フッター削除 | ✅ | 済み |
| R1-28 | 【P0】AutoRun結果が0/0/0で無言成功表示 | ✅ | 根治済み（reporter file出力・タイムアウト自動スケール・部分結果回収・UI3箇所） |
| R1-29 | 【P0】操作不能3画面（ガイド/設定/差分） | ✅ | 済み |
| R1-30 | 【追加】クロールモード選択（自動/URLのみ） | ✅ | 済み |
| R1-31 | 【追加】分析時間の短縮（並列数UI・目安表示） | ✅（並列数UI・目安表示）／ ⚠️高速プリセット`--no-explore`のみスコープ外 | 理由: 状態探索ロジックの核心部（`src/crawler/page_crawler.py` の `crawl_page`）への変更が必要で、本セッションで安定化させたばかりのP0パス（AutoRun実行系）と同じファイル群に触れるため、事故率を下げる目的で意図的に別スプリントに切り出す。実装方針だけ記す: `crawl_page` に `explore_states: bool = True` 引数を追加し、Falseならモーダル/タブ/アコーディオン探索（状態ベース探索フェーズ）とバリデーション実測（dry-run submit）をスキップする。CLIに `--no-explore`、ウィザードに「高速（探索スキップ）」ラジオを追加 |
| R1-32 | 【追加】画面分析の途中停止 | ✅ | 中断ボタン＋プロセス停止済み |
| R1-33 | 【D】QAレポートmd生表示（プレビュー） | ✅（.mdプレビューのレンダラ）→HTML内埋め込みmdは**未対応（S2-3）** | 本文 Sprint 2-3 参照。`web/services/qa/doc_generator.py:392-396` の `html.escape(docs[key])` を `<pre>` に埋め込む処理を、Markdown→HTML変換に置き換える |
| R1-34 | 【E】実行中「n/188件目」進捗表示 | ✅ | 済み |
| R1-35 | 【F】ナビ・ホーム整理（実行履歴導線含む） | ✅ | 済み |

### Round 2（2026-07-05 午後・約29項目）

| # | 指摘 | 状態 | 対応方法 |
|---|---|---|---|
| R2-01 | 【全体】キーボードショートカット拡張 | **未対応（S2-5）** | `static/js/core.js:388-400` の既存ハンドラに追加: `1`〜`9` キー→レポート画面表示中に対応する `.result-tab` をクリック相当（入力欄フォーカス時は無効化する既存ガード踏襲）。`Alt+A`→`switchView('auto-run')`。`Alt+H`→`switchView('run-history')`（C完成後）。`/`→現在表示中の検索input（`#mx-search`等）にフォーカス。`templates/index.html:133-146` のヘルプ表に全項目を追記 |
| R2-02 | 【全体】AutoRunも解析もヒートマップが欲しい | **未対応（S3-4）** | 本文「Sprint 3 - 4. カバレッジヒートマップ」節参照。`src/generator/heatmap_reporter.py` のバケット関数（`_bucket`/`_cell`）を再利用し、解析用は「取得済み/未取得/要ログイン」の3色、AutoRun用は「実行回数」×「成否」の2軸配色で新規ヒートマップHTMLを生成 |
| R2-03 | 【全体】画面一覧も欲しい・機能一覧表も欲しい | **未対応（S2-5）** | 画面一覧は既存 `screens.md`／概要タブが該当するため、レポートのナビゲーションで見つけやすい位置に明示配置（概要タブ冒頭にリンクボタン追加のみ）。機能一覧表は新規: `src/generator/feature_catalog.py` を新設し `generate_features_markdown(pages)` を実装。forms[].action+method の組み合わせ→「フォーム機能」、buttons[]→「操作機能」、transitions→「遷移機能」として集約し `# 機能一覧` 表（機能ID/種別/画面/概要）を出力。`features.md` として `web/routes/report.py` の `files` に追加し、レポート概要タブに新規サブセクションとして表示 |
| R2-04 | 【全体】前回フィードバック未反映への憤り | 本トレーサビリティ表そのものが対応（全量管理） | — |
| R2-05 | 【解析】中断ボタンをスタイリッシュに | **未対応（S1-8）** | 中断ボタンの現行CSSクラスを確認し、`btn-outline-sm btn-danger-outline` 相当（他の小型アウトラインボタンと統一）のサイズ・padding・font-sizeに縮小。幅を `width:auto` にし、目立たない配置（他のボタン群と横並び）に変更 |
| R2-06 | 【解析】認証情報入力パネルが潰れている（画像1） | **未対応（S1-2）** | R1-01と同一根本原因・同一対応（本文 P0-2 参照） |
| R2-07 | 【解析】ライブプレビュー並列処理・速度向上 | ユーザー確認済み: 真意=**AutoRun実行ライブプレビュー** → **未対応（S1-5）** | 本文「Sprint 1-5」参照。`web/routes/crawl.py:142-155` の `/api/live-screenshot`（最新mtimeのPNGを返す）と同じパターンで `GET /api/autorun/live-screenshot?job_id=` を新設し、`output/{domain}/qa_process/test-results/` 配下（Playwright configで`screenshot:'on'`済み・`web/services/playwright_executor.py:288-294`参照）の最新PNGを返す。`static/js/autorun.js` の running_tests 状態表示に `<img>` を追加し1.5秒間隔でポーリング |
| R2-08 | 【解析】画面と仕様の画像が見切れ | **未対応（S2-5）** | R1-06と同一対応（`view-report.js:44-47` の_full.png化） |
| R2-09 | 【解析】ギャラリー一括エクスポート（チェックボックス/全選択/全解除） | **未対応（S2-5）** | `static/js/view-shots.js` の各 `<figure>` サムネイルに `<input type="checkbox">` を追加、ツールバーに「全選択」「全解除」「選択をエクスポート」ボタン。`web/routes/report.py:40-56` の `/download-zip` に optional な `paths`（複数値・カンマ区切りまたは配列）クエリパラメータを追加し、指定時は `base.rglob("*")` 全件でなく該当パスのみzip化（`_safe_output_path`で各パス検証） |
| R2-10 | 【解析】条件マトリクス・技法詳細がジュニアQAに不明瞭 | **未対応（S3-1 B に統合）** | 本文 Sprint 3-1 参照 |
| R2-11 | 【解析】シナリオ表QA観点の具体化（「[hoge]リンク押下で[meta]画面へ」） | **未対応（S2-5）** | `static/js/view-transition.js:176` の固定文字列 `'リンク操作で期待画面へ到達する'` を、同じ行内で既に構築済みの `toTitle`（遷移先画面タイトル）と `eventDetail` を使い `` `${eventDetail}を押すと「${toTitle}」へ遷移する` `` に変更。リンクラベル自体が report.json に無い場合は `eventDetail`（現状固定文字列 `'リンククリック'`、`:182`）をそのまま使い、画面名だけでも具体化する |
| R2-12 | 【解析】遷移図にフローチャート追加 | **未対応（S3-3）** | `static/js/view-transition.js:208-210` にサブタブボタン「フローチャート」追加、`:451-453` のdispatcherに分岐追加、新規 `_flowchartDiagram(rows)` 関数（`_communicationDiagram`と同じ`flowchart LR`パターンを流用）を実装 |
| R2-13 | 【解析】テスト設計タブ新設（技法タブ→画面リスト→ポップアップ） | **未対応（S3-1 B）** | 本文 Sprint 3-1 参照 |
| R2-14 | 【解析】テスト設計はMBT思想で実装 | **未対応（S3-1 B）** | 同上。`src/generator/test_design.py` の `build_test_design()` |
| R2-15 | 【解析】MBTを設定からブラッシュアップ | **未対応（S3-1 B）** | 同上。設定「テスト設計」タブ |
| R2-16 | 【解析】探索カバレッジ削除 | **未対応（S2-1）** | `templates/partials/view-generate.html:299,338`（タブボタン＋パネル）、`static/js/results.js:17`（TAB_DEFS）、`static/js/view-coverage.js`（ファイル自体）を削除。`tests/e2e/test_coverage_tab_e2e.py` を同一コミットで削除または7タブ→6タブのアサーションに更新。バックエンド生成系（`src/generator/heatmap_reporter.py`、`src/capture/coverage.py`）は残置（R2-02のヒートマップで再利用するため） |
| R2-17 | 【解析】履歴・差分を現新比較ツールレベルに | **未対応（S3-2 D）** | 本文 Sprint 3-2 参照 |
| R2-18 | 【AutoRun】深さ・最大ページはオプション化・既定全対象 | **未対応（S1-7）** | R1-08と同一対応 |
| R2-19 | 【AutoRun】テストケース一覧は別画面（直っていない） | **未対応（S2-2 A）** | R1-11と同一対応 |
| R2-20 | 【AutoRun】ローレベルテストケースまで落とし込む | **未対応（S2-2 A）** | R1-12と同一対応 |
| R2-21 | 【AutoRun】成果物が見切れ（画像2） | **未対応（S2-3）** | `static/css/components.css:572-581` の `.qa-output-item-label` の `white-space:nowrap;overflow:hidden;text-overflow:ellipsis` を `white-space:normal;overflow:visible` に変更し折返しを許可。併せて `title="${label}"` 属性を追加してホバーで全文表示できるようにする |
| R2-22 | 【AutoRun】成果物をSDLC/TPI NEXT思想でカテゴライズ | **未対応（S2-3）** | `static/js/autorun.js` の `AUTORUN_OUTPUT_LABELS`（成果物キー→ラベルの辞書）に `category` を追加し、計画/分析/設計/実装/実行/レポートの6グループに分類。成果物一覧描画箇所（`_autorunOutputSummary`等）をグループ見出し付きのリストに変更 |
| R2-23 | 【AutoRun】テスト実行時のライブプレビュー未適用 | **未対応（S1-5）** | R2-07と同一項目（重複報告）。本文Sprint 1-5で統合対応 |
| R2-24 | 【AutoRun】自動テストが全て失敗（Playwright環境） | **✅対応済み（S1-1）** | 本文 P0-1 参照。`_ensure_pw_env`をPython側`playwright`バージョンにピン止め＋バージョン不一致時の再構築＋ブラウザ実在検証＋`npx playwright install chromium`自動修復、`failure_classifier.py`に環境エラーパターン追加＋重複失敗の1件集約を実装。フレッシュ環境／バージョン不一致環境の両方で実際にAutoRunを完走させ確認済み |
| R2-25 | 【AutoRun】spec.tsではなくHTMLレポートも | **未対応（S2-2 A）** | `web/services/qa/testcase_docs.py` の `generate_testcases_html()` で対応（本文参照） |
| R2-26 | 【AutoRun】QAレポートが素の.mdで読みにくい（画像4） | **未対応（S2-3）** | R1-33と同一対応（`doc_generator.py:392-396`のmd→HTML変換） |
| R2-27 | 【実行履歴】一般的な実行履歴に（AutoRunだけでない） | **未対応（S2-4 C）** | 本文 Sprint 2-4 参照。`web/services/usage_tracker.py` に `record_autorun()` を追加し、`GET /api/history/runs` で crawl/comparison/ux_review/autorun の全種別を統合表示 |
| R2-28 | 【品質観点/観点管理】前回の指摘が反映されていない | **未対応（S1-3／S1-4）** | R1-15〜R1-19と同一（このセッションで初めて着手） |
| R2-29 | 【設定】APIキー+モデル+疎通確認を1タブに・モデルタブ不要・クロール規定値 | **未対応（S1-6）** | `templates/partials/view-settings.html` の `model` タブ（5-8行目付近、独立タブ）を削除し、`OPENAI_MODEL`セレクトを `api` タブ内（APIキー入力の直下）に統合。新規 `POST /api/settings/test-connection` を `web/routes/settings.py` に追加し、`src/llm/` のプロバイダ抽象を使ってOpenAI APIに軽量なping（例: モデル一覧取得や最小トークンのdry-run）を送り、成功/失敗をUIの `#api-status-msg`（既存の未使用要素、`view-settings.html:25`）に表示。クロール既定値タブ（`crawl`）はそのまま存続 |

### ドキュメント関連（前ターン受領）

| # | 指摘 | 状態 | 対応方法 |
|---|---|---|---|
| D-01 | ドキュメント類を全てアップデート | **未対応（S3-5）** | 本文 Sprint 3-5 参照 |
| D-02 | 非エンジニア向け日英.md作成 | **未対応（S3-5）** | `docs/GUIDE_ja.md`（新規）: 専門用語なしで「何ができるか」「使い方4ステップ」「よくある質問」を平易な日本語で。`docs/GUIDE_en.md`: 同内容の英語版。`README_en.md`: ルートREADMEの英語版（開発者向けセットアップ手順も含む） |
| D-03 | ビジネスモデル大会関連を全て削除 | **未対応（S3-5）** | 削除対象・編集対象は本文「Sprint 3-5」節に列挙済み（`git rm`で削除、5ファイルはリンク修繕のみの編集） |

**カバレッジ集計**: 全67項目中、✅対応済み（このトレーサビリティ表作成時点）24件／未対応43件（すべて本計画のSprint 1〜3で対応方法を明記済み）／⚠️明示的スコープ外1件（R1-31の高速プリセット`--no-explore`：理由と実装方針を上表に明記）。

**本表の運用**: 各Sprintのコミット完了時、該当行の状態を ✅ に更新し、対応内容の要約（コミットハッシュ or PR番号）を追記すること。
