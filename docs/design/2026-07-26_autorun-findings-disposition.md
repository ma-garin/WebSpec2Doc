# AutoRun 指摘52件の処理台帳（2026-07-26）

出典: `2026-07-26_autorun-usability-consolidated.md`（5レンズ独立評価）＋レンズ0（初回評価 S1〜S8）
コミット: `19f7a89`（承認ゲート）/ `8bc4a41`（用語・復旧・視覚）/ `2117ffe`（根拠と記録の可視化）

判定の凡例: **対応** = コード変更で解消 / **既に解消** = 並行作業や現行実装で既に解消済み / **仕様判断** = 直さない判断とその理由

## レンズ0（初回評価）

| # | 指摘 | 判定 | 内容 |
|---|---|---|---|
| S1 | 件数矛盾（未確認4件 vs 要確認0/1件） | 既に解消 | 149 = 要確認6 + 自動承認143 で整合済み |
| S2 | 同一項目の二重表示 | 既に解消 | 段階別画面が廃止され要確認1本に集約 |
| S3 | 承認CTAの競合 | 対応 | ラベルを実効果に合わせ、実行経路を1本に |
| S4 | 段階数 11/8/6 | 対応 | 受付の工程一覧を STAGE_DEFINITIONS の8段階に統一 |
| S5 | 相談ボタン重複 | 対応 | カード内はリンク化（可視ボタン42→32、青CTAは1つ） |
| S6 | 事前確認の説明が長く警告的 | 対応 | 「そのまま開始で構いません」を先頭に、任意である旨を明示 |
| S7 | FABが本文に重なる | 既に解消 | 現行レイアウトで重なりを確認できず |
| S8 | 初回ツアーが入力欄を覆う | 既に解消 | 現行ビルドでツアーは表示されない |

## レンズ1（初回利用者）

| # | 指摘 | 判定 | 内容 |
|---|---|---|---|
| F1-1 | 文書駆動でもURL必須 | 対応 | 開始前チェックで理由を提示（`autorunStartBlockReason`） |
| F1-2 | 「全自動」なのに承認が必要 | 対応 | 「途中2回だけ止まります」を受付に明記 |
| F1-3 | 既定値（深さ10・500画面）が折りたたみ内 | 対応 | 常時表示に変更 |
| F1-4 | URL未入力でも開始が押せる | 対応 | 押す前に無効化＋理由表示 |
| F1-5 | 事前確認を押すべきか判断できない | 対応 | 任意であることを明示、名称を「先に到達確認」に |
| F1-6 | サイドバーに現在地がない | 既に解消 | 「受付」が is-active |
| F1-7 | 中止できることが受付に書かれていない | 対応 | 中止に確認ダイアログを追加し、失うものを明示 |
| F1-8 | ログイン要求の事前告知がない | 対応 | 「途中2回だけ止まります（ログインが必要なとき／実行条件の確認）」 |
| F1-9 | 観点セット読込失敗の理由が遠い | 対応 | ボタン直下に理由を表示 |
| F1-10 | モード説明の二重表示 | 対応 | 選択中表示を支援技術向けのみに |
| F1-11 | 工程数の不一致 | 対応 | S4と同じ |

## レンズ2（段階承認の判断者）

| # | 指摘 | 判定 | 内容 |
|---|---|---|---|
| F2-1 | 未確認件数が2系統 | 既に解消 | S1と同じ |
| F2-2 | 「確定する」の実効範囲がラベルより広い | 対応 | ラベルを「確認済みとして記録する」に。実行条件の確定は人の確認を代替しないよう、未確認項目の approved 書き換えを停止 |
| F2-3 | 主導線と確定ボタンの活性が矛盾 | 対応 | S3と同じ |
| F2-4 | 自動承認分が畳まれたまま承認成立 | 対応 | 畳んだ状態でも判定根拠（実測に基づく低リスク・人の確認を求めていない旨）を常時表示 |
| F2-5 | 出所バッジが1件のみ | 既に解消 | 現行は全行に確信度バッジ＋高リスクバッジを描画 |
| F2-6 | 同一項目の二重掲載 | 既に解消 | S2と同じ |
| F2-7 | 承認しない場合どうなるか不明 | 対応 | 「このまま実行できるが『人の確認を経ていない』と成果物に記録される」と明示。未チェック＝却下相当の記録が残る |
| F2-8 | 確定後の記録先への導線がない | 対応 | 「記録を見る」でその場に監査記録を展開（実機で241件の表示を確認） |

## レンズ3（異常系）

| # | 指摘 | 判定 | 内容 |
|---|---|---|---|
| F3-1 | 承認が2時間で自動前進 | 対応 | 未確認事項として記録し成果物へ。画面に残り時間と結果を明示 |
| F3-2 | ログイン待ち30分で無言スキップ | 対応 | 同上（「あと約29分で未ログインのまま続行」） |
| F3-3 | 復旧手段が最初からやり直すのみ | 対応 | 「同じ条件でやり直す」を追加 |
| F3-4 | 0画面でも失敗にならない | 対応 | 失敗として扱う |
| F3-5 | 生成失敗が未着手と同じ表示 | 対応 | 未確認事項として記録 |
| F3-6 | 中止に確認がない | 対応 | 確認ダイアログを追加 |
| F3-7 | スキップ範囲が未検証として集約されない | 対応 | 未観測領域を未確認事項へ集約 |
| F3-8 | エラー文言が非表示要素へ | 対応 | 受付表示中のみそこへ出す |
| F3-9 | タイムアウトが次の操作を示さない | 対応 | 失敗時の主導線バーに原因＋やり直しを表示 |

## レンズ4（情報設計）

| # | 指摘 | 判定 | 内容 |
|---|---|---|---|
| F4-1 | 未確認件数の割れ | 既に解消 | S1と同じ |
| F4-2 | 前進操作が2系統 | 対応 | S3と同じ |
| F4-3 | 段階数 11/8/6 | 対応 | S4と同じ |
| F4-4 | 工程名が名寄せできない | 対応 | 受付の工程名を STAGE_DEFINITIONS に一致させた |
| F4-5 | 画面数がログ7件・本文6画面で食い違う | 対応 | 差と理由（要ログインで対象外）を両方のログに明示 |
| F4-6 | 「停止」が状態と操作で衝突 | 対応 | 待機中は「入力待ち」に |
| F4-7 | 取得行為に5語が混在 | 対応 | 「到達確認 → 観測」に統一 |
| F4-8 | 「確認」が4用法で衝突 | 対応 | 「要確認」「確認済み」の2語に固定 |
| F4-9 | 「観点」の修飾語が画面ごとに違う | 対応 | 観点セットにツールチップを付け、観点管理との関係を明示 |
| F4-10 | ページと画面の混在 | 対応 | 「画面」に統一 |
| F4-11 | 成果物の呼称が4種 | 対応 | 「成果物」に統一 |
| F4-12 | 件数の二重表示 | 既に解消 | 該当コードが段階別画面の廃止で消滅 |
| F4-13 | 段階名の略称化 | 対応 | 「ケース」表記を廃止 |

## レンズ5（視覚）

| # | 指摘 | 判定 | 内容 |
|---|---|---|---|
| F5-1 | 操作要素42個・青CTA4個 | 対応 | 32個・青CTA1個（「実行する」）に |
| F5-2 | 前進CTAの競合 | 対応 | S3と同じ |
| F5-3 | 件数の不一致 | 既に解消 | S1と同じ |
| F5-4 | FABが本文に重なる | 既に解消 | S7と同じ |
| F5-5 | 初回ツアーが覆う | 既に解消 | S8と同じ |
| F5-6 | 相談入口が6個 | 対応 | S5と同じ |
| F5-7 | 進捗表現が3系統 | 対応 | S4と同じ |
| F5-8 | モード説明の二重表示 | 対応 | F1-10と同じ |
| F5-9 | ホームに開始系CTAが5個 | 既に解消 | AutoRun受付の開始系CTAは1個 |
| F5-10 | opacity:.45 でコントラスト不足 | 対応 | 色で無効を示す方式に変更 |
| F5-11 | サイドバーに「テストケース」が2個 | 既に解消 | 現行サイドバーは受付/過去履歴/ガイド/参考/設定の5項目 |

## 集計

| 判定 | 件数 |
|---|---|
| 対応（コード変更） | 40 |
| 既に解消 | 12 |
| 仕様判断（直さない） | 0 |
| 合計 | 52 |

## 追加検証（2026-07-26 19:20〜19:35）

指摘外だった画面を実機で通し、以下を確認・修正した。

| 対象 | 結果 |
|---|---|
| 段階2〜8の個別画面 | **存在しないことを確認**。段階ナビは常時非表示、段階詳細を描く関数も不在で到達不能だった。マークアップごと削除し、契約テストで再発を防止 |
| 実行結果レポート（8タブ） | 全タブに内容あり・空表示なし。外部フォント読み込みがCSPでブロックされコンソールエラーになっていたため撤去。「6ページ（正規化後6画面）」の2単位表記を画面へ統一 |
| 失敗経路（到達不能URL） | 失敗として扱われ、原因＋「同じ条件でやり直す」が表示されることを確認。内部ファイル名を出していたメッセージを利用者向けに修正 |
| 過去履歴 | 種別タブの「解析」だけが動詞だったため機能名へ統一 |

視覚的複雑性は、DOMから算出できる代理指標に上限を設けたE2E（`tests/e2e/test_visual_complexity_e2e.py`）で固定した。受付画面の実測は可視要素169・操作要素21・主要CTA 1・色11・情報密度27%。

## 視覚的複雑性の実測（画像ベース）

DOM の要素数は代理でしかないため、描画結果そのものから測る実装を追加した
（`web/services/visual_complexity.py`、Reinecke et al. 2013 が用いた画像特徴量の再実装）。

| 画面 | 圧縮率 | エッジ密度 | colorfulness |
|---|---|---|---|
| 改修前の段階承認画面（05） | 0.0801 | 0.0429 | 24.69 |
| **改修後の要確認画面（06）** | **0.0680** | **0.0327** | 17.79 |
| 受付画面（03） | 0.0829 | 0.0457 | 37.68 |
| 実行結果レポート（08） | 0.0616 | 0.0281 | 13.19 |

要確認の1本化とCTA整理により、**複雑性が約2割低下**したことを数値で確認した。
上限は E2E（`tests/e2e/test_visual_complexity_e2e.py`）に設定済み。

## 用語統一の適用範囲

前回は AutoRun 内に閉じていたが、過去履歴が両システムの実行を1画面に並べるため
矛盾が露出していた。ドキュメント作成システム側も「観測」へ統一した。
ホーム画面の実測で「解析」「クロール」は 11 箇所 → **0 箇所**。

関数名・要素ID・APIパス・CLIオプションは互換のため据え置き。
参考文献の学術用語（データフロー追跡の "クロール" 等）は原文を保持する。

## 残る限界

- 画像特徴量は原著の**知覚モデルそのものではなく再実装**である。絶対値で美しさを主張はできず、同じ画面の悪化検知に用いる。
- 5レンズは同一の静止画とソースを見ているため、素材由来の見落としは原理的に残る（追加検証で実行結果ページ・失敗経路・段階ナビの3領域は解消済み）。
