# AutoRun 入力後画面の再設計（案）

対象: URL入力／文書駆動の**次の画面から実行結果まで**すべて
起点: 2026-07-26 のFB（アシスタント常駐・仕様書作成の不可視性・成果物一覧のデザイン・進行不能）
状態: **レビュー待ち。承認前に実装へ進まない。**

---

## 1. 何が壊れているか（診断）

FBを症状ではなく原因で分類する。

| # | 症状（FB） | 原因 | 種別 |
|---|---|---|---|
| F1 | 右のQAアシスタントが常駐して邪魔 | 使用頻度の低い機能に画面幅の1/3を常時割り当てている | レイアウト |
| F2 | 「仕様書作成」で何をしているか見えない | 進捗が「ステップ名＋スピナー」のみ。**取得中URL・検出結果・遭遇した障害が一切出ない** | 可視性 |
| F3 | ログインが必要でも何も聞かれない | ログイン壁を検出しても停止せず、無言で「取得できた範囲」で進む | 制御フロー |
| F4 | 成果物一覧のデザインが受け入れられない | 12件を均質な縦積みリスト＋同一の「プレビュー」ボタンで並べており、**どれが重要か・何が分かるかの情報が0** | 情報設計 |
| F5 | この画面から先に進めない | 「確定する」がリスト最下部。要確認5件＋自動承認149件の下で画面外 | 導線（**修正済み: sticky化**） |

共通する根本原因は1つ。
**「AutoRunが今なにをしていて、人は次に何をすればよいか」が画面のどこにも常設されていない。**
個々の部品は揃っているが、進行の主導線が無いため、人は毎回スクロールして探すことになる。

---

## 2. 設計方針

1. **主導線を1本にする** — 画面上部に「現在地」と「次の一手」を固定表示する。人が探す状態を作らない
2. **常駐は主導線だけ** — アシスタントも成果物も、必要になったとき呼び出す
3. **黒箱を作らない** — 待ち時間のある処理は、いま何を取得しているかを逐次見せる
4. **止まるべきときは止まる** — ログイン壁・robots制限・タイムアウトは「無言で継続」せず、人に判断を返す
5. **一覧は数字で語らせる** — ファイル名の羅列ではなく、その成果物で何が分かったかを1行と主要数値で示す

---

## 3. 個別の設計

### 3.1 QAアシスタント（F1）

- 右カラム常駐を**廃止**。メインカラムを全幅に戻す
- 呼び出し口は2つに限定する
  - 各項目の「アシスタントに相談」ボタン（現状どおり）
  - 画面右下の常設FAB（1つだけ）
- 開くと**右からのドロワー**（幅420px・オーバーレイ）。閉じると完全に消える
- 相談対象の項目タイトルをドロワー先頭に固定表示し、何について話しているかを見失わせない

### 3.2 仕様書作成の可視化（F2）

「仕様書生成」ステップ実行中、以下を**ライブ表示**する。

```
仕様書を作成しています（3/9ページ）
├ 取得済み  /index.html        フォーム0件・リンク6件
├ 取得済み  /login.html        フォーム1件（必須2）
└ 取得中    /mypage.html       …
```

- 現在取得中のURLを必ず出す
- 各ページで検出したもの（フォーム数・必須項目数・リンク数）をその場で出す
- スキップしたURLは理由つきで出す（robots Disallow ／ 404 ／ タイムアウト ／ 対象外ドメイン）
- **「取得できなかった」を隠さない。**空欄で通さない

### 3.3 ログイン壁の扱い（F3）

ログインが必要と判定した時点で、**進行を止めて**人に返す。

判定条件（いずれか）:
- ログインフォーム（password型input）を持つページへリダイレクトされた
- 401/403 が返った
- 対象URLに到達したが、指定した継続条件を満たさない

提示するもの:
```
このサイトはログインが必要です。
取得できた範囲: 3画面（未ログインで到達可能な範囲のみ）
[ ログイン情報を設定して続ける ]  [ 未ログインのまま進む ]
```

- 既定は**どちらも選ばせる**。勝手に片方へ倒さない
- 「未ログインのまま進む」を選んだ場合、成果物に「未ログイン範囲のみ」を明記する

### 3.4 成果物一覧（F4）

現状の縦積みリストを廃止し、**3つの束**に再編する。

| 束 | 何のための束か | 含めるもの |
|---|---|---|
| いま見るべきもの | 結論と判断材料 | テスト結果レポート・横断レビュー・仕様ドリフト |
| 中身を確かめるもの | 根拠の確認 | 仕様書HTML・テストケース・テスト設計・モデルグラフ |
| 持ち出すもの | 提出・再利用 | Excel・JSON・Playwright候補・証跡パック |

各項目は「名前＋プレビューボタン」ではなく、次を持つ。

```
テストケース                                    47件
 うち要確認 5件 / 自動承認 42件・文書要件の94%をカバー
```

- カード全体をクリックでプレビュー（ボタンの繰り返しを廃止）
- 主要数値を必ず1つ出す。数値が出せないものは「未生成」と明示する
- 中身が空の成果物は**灰色にして最後尾へ**（並びを固定して空箱を上位に置かない）

### 3.5 進行の主導線（F5・全体）

画面最上部に固定バーを置く。スクロール位置に関係なく常に見える。

```
STEP 1 テスト目的  ─  未確認 0件  ─  [ 確定して次へ ]
```

- 現在のステップ名／残タスク数／次の操作の3つだけ
- 次の操作ボタンは、押せない場合**理由をボタン脇に出す**（「未確認が2件あります」）
- 左サイドの進捗リストは現在地の表示に徹し、操作は主導線に集約する

---

## 4. 影響範囲

| ファイル | 変更内容 |
|---|---|
| `templates/partials/view-auto-run.html` | 右カラム廃止・固定主導線の追加・成果物セクションの再構成 |
| `static/js/autorun-chat.js` | 常駐パネル → ドロワー化 |
| `static/js/autorun-review.js` | 確定操作を主導線へ移設 |
| `static/js/autorun-stages.js` | 各段階の残タスク数を主導線へ供給 |
| `static/js/autorun.js` | クロール進捗のライブ表示・ログイン壁の停止処理 |
| `web/routes/auto_run.py` | ページ単位の進捗イベント送出・ログイン壁検出時の停止と再開API |
| `static/css/components.css` ほか | レイアウト変更に伴う調整 |
| `tests/e2e/snapshots/*` | 画面変更に伴う再取得が必要 |

---

## 5. 未確定事項（レビューで決めたいこと）

1. **ログイン壁で止める既定** — 止めて聞く／未ログインで進んで後から知らせる、どちらを既定にするか
2. **成果物の束の切り方** — 上記3束でよいか。「持ち出すもの」を分ける必要があるか
3. **主導線バーの位置** — 画面最上部固定か、コンテンツ直上の追従か
4. **着手順** — F2/F3（可視性・制御フロー）とF1/F4（レイアウト・情報設計）のどちらを先に出すか

---

## 6. この案の弱点

- **F2/F3はバックエンドの進捗イベント設計を伴うため、見た目の変更より重い。** 影響範囲の見積もりが `web/routes/auto_run.py` の実装確認前であり、粗い
- 成果物カードに出す「主要数値」は、成果物ごとに算出元が異なる。**すべての成果物で数値を出せるかは未確認**
- E2Eスナップショットの再取得が必須になるため、UI変更ゲート（`.ui-verified`）の通過コストを織り込んでいない
