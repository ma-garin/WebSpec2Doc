# fetch 経路のエラー処理点検（P0-3）

- 対象: `static/js/*.js` の `fetch(` **87 経路**
- 判定はヒューリスティック（構文解析ではない）。誤判定は `noqa: fetch-error` を行末に付けて除外できる。

| 判定 | 件数 | 意味 |
|---|---|---|
| ok | 84 | 捕捉し、利用者に見える形で伝えている |
| silent | 0 | 捕捉はするが画面に何も出さない |
| no_catch | 0 | 捕捉していない（未処理の失敗） |
| ignored | 3 | 意図的に除外 |

## 捕捉していない経路

（なし）

## 捕捉するが画面に出ない経路

（なし）
